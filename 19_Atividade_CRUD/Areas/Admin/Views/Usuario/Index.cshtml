@model IEnumerable<_19_Atividade_CRUD.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Usuários</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-dark">Adicionar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imagem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Login)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Senha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imagem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Senha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UsuarioId" class="btn btn-outline-secondary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UsuarioId" class="btn btn-outline-secondary">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UsuarioId" class="btn btn-outline-secondary">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
